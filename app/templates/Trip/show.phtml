{block #header}
	<strong class="departure">From</strong>
	<span class="trip departure">{$trip->departure}</span>
	
	<strong class="arrival">To</strong>
	<span class="trip arrival">{$trip->arrival}</span>
{/block}

{block #js}
	<script type="text/javascript">
		var isTripPage = true;
	</script>
{/block}

{block #content}
	<p><a href="{link Homepage:}">← Plan new trip</a> | <a href="{link this}">permalink</a></p>
	<p>The trip will take <strong>{$trip->duration |duration}</strong> to complete and its distance is <strong>{$trip->distance |distance}</strong>.</p>
	
	<div>
		<div id="article">{!$article->text}</div>
	</div>
	
	<div>
		<ol id="directions">
			{foreach $trip->steps as $step}
				<li>{!$step->instructions} ({$step->distance |distance}).</li>
			{/foreach}
		</ol>
	</div>
	
	<div>
		<a id="pois-link" class="unwrap plus" href="#">Points of interest</a>
		<p id="showing-pois">Showing POIs in the map.</p>
	</div>
	
	<div>
		<div id="events">
		{if count($events) > 0}
			{foreach $events as $event}
				<h3><a href="{$event->url}">{$event->title}</a> {$event->date->format('j. n. Y')}</h3>
				<div class="small">
					<p><strong>Where:</strong> <a href="{$event->venue->url}">{$event->venue->name}</a></p>
					{!$event->description}
				</div>
				{sep}<hr />{/sep}
			{/foreach}
		{else}
			<p>No events available.</p>
		{/if}
		</div>
	</div>
	
	<div>
		<div id="flights">
			{if count($flights) > 0}
				{foreach $flights as $flight}
					<h3><a href="http://kayak.com{$flight->getBook()}">{$flight->price} {$flight->currency}</a></h3>
					{foreach $flight->legs as $leg}
						<ul>
							<li>Airline: {$leg->getAirlineDisplay()}</li>
				            <li>From: {$leg->getOrig()} {$leg->getDepart()}</li>
				            <li>To: {$leg->getDest()} {$leg->getArrive()}</li>
				            <li>Duration: {$leg->getDurationTime()}</li>
				            <li>Cabin: {$leg->getCabin()}</li>
				        </ul>
					{/foreach}
				{/foreach}
			{else}
				<p>No flights available.</p>
			{/if}
		</div>
	</div>
