{block }

{block #content}

<p>List of saved trips.</p>

{if count($trips) > 0}
	<ul>
	{foreach $trips as $trip}
		<li><a href="{link show $trip->id}">From <strong>{$trip->departure}</strong> to <strong>{$trip->arrival}</strong></a></li>
	{/foreach}
	</ul>
{else}
	<p>No saved trips yet.</p>
{/if}